{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "python manage.py migrate --noinput"
    ],
    "startCommand": "python manage.py collectstatic --noinput && gunicorn ngoma_backend.wsgi --log-file -"
  }
}
